<Window x:Class="TVS_Player.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TVS_Player"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280"  MinHeight="480" MinWidth="800"
        Loaded="Window_Loaded" WindowStyle="None"
        ResizeMode="CanResizeWithGrip" AllowsTransparency="True" ShowInTaskbar="True" Style="{StaticResource Font}" VisualTextRenderingMode="ClearType" Background="Transparent">
    <Window.Resources>
        <Storyboard x:Key="BlurBackground">
            <DoubleAnimation Storyboard.TargetProperty="Radius" To="50" Storyboard.TargetName="GridBlur" DecelerationRatio="0.75" Duration="0:0:0.200" />
        </Storyboard>
        <Storyboard x:Key="UnBlurBackground">
            <DoubleAnimation Storyboard.TargetProperty="Radius" To="0" Storyboard.TargetName="GridBlur" DecelerationRatio="0.75" Duration="0:0:0.200" />
        </Storyboard>
    </Window.Resources>
    <Border CornerRadius="3"  Background="{DynamicResource Background}">
        <Grid>
            <!--#region TopBar-->
            <Grid x:Name="TopBar" Height="45" Margin="5" VerticalAlignment="Top" Panel.ZIndex="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="BackBar" Grid.Column="0" Margin="10,0" Width="107" Background="{StaticResource TopBarBackgrdound}" CornerRadius="11" VerticalAlignment="Center" HorizontalAlignment="Left" Height="26" MouseEnter="HandPointer" MouseLeave="NormalPointer">
                    <Border.Effect>
                        <DropShadowEffect Opacity="0.5" Direction="300" BlurRadius="10" />
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" MaxWidth="7" x:Name="BackButtonWidth"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Background="Transparent" x:Name="BackButton" Opacity="0" MouseLeftButtonUp="BackButton_MouseLeftButtonUp">
                            <Image Source="{StaticResource Back}" RenderOptions.BitmapScalingMode="HighQuality" Margin="8.5" />
                            <Border HorizontalAlignment="Right" Background="#AAAAAA" Width="1" Margin="0,2,0,2" CornerRadius="1" />
                        </Grid>
                        <Grid Grid.Column="1" MouseDown="MoveWindow" Background="Transparent" >
                            <TextBlock HorizontalAlignment="Left" Margin="5,0" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TopBarTextColor}">TVS_Player</TextBlock>
                        </Grid>
                    </Grid>
                </Border>
                <Grid Grid.Column="1" Background="Transparent" MouseEnter="TopBar_MouseEnter" MouseLeave="TopBar_MouseLeave">
                    <Border Background="{StaticResource TopBarBackgrdound}" CornerRadius="11" x:Name="SearchBar" Height="26" Margin="10,-100,10,0">
                        <Border.Effect>
                            <DropShadowEffect Opacity=".75" Direction="320" BlurRadius="10" />
                        </Border.Effect>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="35"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" Background="Transparent">
                                <Image Source="{StaticResource Search}" RenderOptions.BitmapScalingMode="HighQuality" Margin="8.5" />
                            </Grid>
                            <Grid Grid.Column="1">
                                <Border HorizontalAlignment="Right" Background="#AAAAAA" Width="1" Margin="0,2,-1,2" CornerRadius="1" />
                                <TextBox Background="{StaticResource TopBarBackgrdound}" FontSize="16" Foreground="{StaticResource TopBarTextColor}" BorderThickness="0" Margin="5,2,5,0"></TextBox>
                                <Border HorizontalAlignment="Left" Background="#AAAAAA" Width="1" Margin="-1,2,0,2" CornerRadius="1" />
                            </Grid>
                            <Grid Grid.Column="2">

                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
                <Border Grid.Column="2" Margin="10,0" Background="{StaticResource TopBarBackgrdound}" CornerRadius="11" VerticalAlignment="Center" HorizontalAlignment="Right" Width="100" Height="26">
                    <Border.Effect>
                        <DropShadowEffect Opacity="0.5" Direction="300" BlurRadius="10" />
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Background="Transparent" x:Name="MinimizeButton" MouseLeftButtonUp="MinimizeButton_MouseLeftButtonUp" MouseEnter="HandPointer" MouseLeave="NormalPointer">
                            <Image Source="{StaticResource Minimize}" RenderOptions.BitmapScalingMode="HighQuality" Margin="8.5" />
                        </Grid>
                        <Grid Grid.Column="1" Background="Transparent" x:Name="MaximizeButton" MouseLeftButtonUp="MaximizeButton_MouseLeftButtonUp" MouseEnter="HandPointer" MouseLeave="NormalPointer">
                            <Border HorizontalAlignment="Right" Background="#AAAAAA" Width="1" Margin="0,2,-1,2" CornerRadius="1" />
                            <Image Source="{StaticResource Maximize}" RenderOptions.BitmapScalingMode="HighQuality" Margin="8.5" />
                            <Border HorizontalAlignment="Left" Background="#AAAAAA" Width="1" Margin="-1,2,0,2" CornerRadius="1" />
                        </Grid>
                        <Grid Grid.Column="2" Background="Transparent" x:Name="ExitButton" MouseLeftButtonUp="ExitButton_MouseLeftButtonUp" MouseEnter="HandPointer" MouseLeave="NormalPointer">
                            <Image Source="{StaticResource Close}" RenderOptions.BitmapScalingMode="HighQuality" Margin="8.5" />
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            <!--#endregion-->

            <Grid>
                <Grid>
                    <Grid.Effect>
                        <BlurEffect x:Name="GridBlur" Radius="0" />
                    </Grid.Effect>
                    <Frame x:Name="MainContent" NavigationUIVisibility="Hidden" Background="Transparent" Panel.ZIndex="1" />
                    <Image x:Name="MainContentOld" Panel.ZIndex="2" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
                <Grid Margin="2,0,-2,0">
                    <Grid x:Name="TopContent" Panel.ZIndex="10" />
                </Grid>

            </Grid>
            
        </Grid>
    </Border>
</Window>
