<UserControl x:Class="TVS_Player.EpisodePreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TVS_Player"
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="320">
    <Border CornerRadius="5" Margin="5">
        <Grid>
            <Border x:Name="Test" CornerRadius="5"  Background="{StaticResource TopBarBackground}"/>
            <StackPanel>
                <StackPanel.OpacityMask>
                    <VisualBrush Visual="{Binding ElementName=Test}" />
                </StackPanel.OpacityMask>
                <Image x:Name="EpisodeThumbnail" RenderOptions.BitmapScalingMode="HighQuality" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
